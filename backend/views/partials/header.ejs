<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
    <link href="/bootstrap/css/bootstrap.min.css" rel="stylesheet">
    <link href="/css/global.css" rel="stylesheet">
    <style>
      
    </style>
</head>
<body>
<nav class="navbar navbar-expand-lg custom-navbar">
  <div class="container-fluid">
    <!-- Logo -->
    <a class="navbar-brand" href="/<% if (isLoggedIn) { %>?userId=<%= user.userId %><% } %>">
      <img src="https://img.icons8.com/ios-filled/40/000000/sushi.png" alt="Logo" class="me-2" style="height:36px;">
      CloudKitchen Pro
    </a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNav">
      <ul class="navbar-nav mx-auto mb-2 mb-lg-0">
        <% if (isLoggedIn && user.role === 'chef') { %>
        <!-- Recipes Dropdown -->
        <li class="nav-item dropdown">
          <a class="nav-link dropdown-toggle" href="#" id="recipesDropdown" role="button" data-bs-toggle="dropdown" aria-expanded="false">
            Recipes
          </a>
          <ul class="dropdown-menu">
            <li><a class="dropdown-item" href="/recipe/recipes-<%= STUDENT_ID %><% if (isLoggedIn) { %>?userId=<%= user.userId %><% } %>">My Recipes</a></li>
            <li><a class="dropdown-item" href="/recipe/add-<%= STUDENT_ID %><% if (isLoggedIn) { %>?userId=<%= user.userId %><% } %>">Add Recipe</a></li>
            <li><a class="dropdown-item" href="/recipe/availability-<%= STUDENT_ID %><% if (isLoggedIn) { %>?userId=<%= user.userId %><% } %>">Recipe Availability</a></li>
          </ul>
        </li>
        <% } %>
         <% if (isLoggedIn) { %>
        <!-- Inventory Dropdown -->
        <li class="nav-item dropdown">
          <a class="nav-link dropdown-toggle" href="#" id="inventoryDropdown" role="button" data-bs-toggle="dropdown" aria-expanded="false">
            Inventory
          </a>
          <ul class="dropdown-menu">
            <li><a class="dropdown-item" href="/inventory/inventories-<%= STUDENT_ID %><% if (isLoggedIn) { %>?userId=<%= user.userId %><% } %>">List Inventory</a></li>
            <li><a class="dropdown-item" href="/inventory/add-<%= STUDENT_ID %><% if (isLoggedIn) { %>?userId=<%= user.userId %><% } %>">Add Inventory</a></li>
            <li><a class="dropdown-item" href="/inventory/alerts-<%= STUDENT_ID %><% if (isLoggedIn) { %>?userId=<%= user.userId %><% } %>">Inventory Alerts</a></li>
            <li><a class="dropdown-item" href="/inventory/insights-<%= STUDENT_ID %><% if (isLoggedIn) { %>?userId=<%= user.userId %><% } %>">Inventory Insights</a></li>
          </ul>
        </li>
        <% } %>
        <!-- Reports Dropdown -->
        <% if (isLoggedIn && user.role === 'admin') { %>
        <li class="nav-item dropdown">
          <a class="nav-link dropdown-toggle" href="#" id="reportsDropdown" role="button" data-bs-toggle="dropdown" aria-expanded="false">
            Reports
          </a>
          <ul class="dropdown-menu">
            <li><a class="dropdown-item" href="/report/analytics-<%= STUDENT_ID %><% if (isLoggedIn) { %>?userId=<%= user.userId %><% } %>">View Reports</a></li>
          </ul>
        </li>
        <% } %>
      </ul>
      <div class="d-flex align-items-center">
        <% if (isLoggedIn && user) { %>
          <span class="navbar-text me-3 d-none d-lg-block" style="font-weight:500;">
            Welcome, <%= user.fullname %>
          </span>
          <a href="/user/logout-<%= STUDENT_ID %>?userId=<%= user.userId %>" class="btn btn-outline-danger btn-md">Logout</a>
        <% } else { %>
          <a href="/user/login-<%= STUDENT_ID %>" class="btn btn-dark btn-md">Get Started</a>
        <% } %>
      </div>
    </div>
  </div>
</nav>
<!-- ...page content starts here... -->